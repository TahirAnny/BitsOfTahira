name: React CI

# This workflow runs on every push and pull request to main or any branch
on:
  push:
    branches:
      - main
      - '**'   # all branches
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Install dependencies
        run: npm install

      - name: Build
        run: npm run build

      # Optional: Run tests if you have them
      # - name: Run tests
      #   run: npm test

      # Optional: Lint your code if you use ESLint
      # - name: Lint code
      #   run: npm run lint
